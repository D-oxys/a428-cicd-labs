pipeline {
    agent any  // Menggunakan agen Jenkins yang tersedia

    stages {
        stage('Build') {
            steps {
                sh 'npm install'  // Langkah-langkah build Anda di sini
            }
        }
    }
}
